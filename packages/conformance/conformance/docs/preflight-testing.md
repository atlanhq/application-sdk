# Preflight conformance

Current policy: the SDK deprecates `PreflightStatus.PARTIAL`. Removal lands in the first minor release after the reference apps stop returning it, anchored at v3.40.0; the gate emits a `DeprecationWarning` when a handler returns it. A PARTIAL verdict is reported by B001 as a deprecated-enum-member read, not by a preflight rule: a preflight-specific rule would put a second WARN on the same line. F016 scenarios accept PARTIAL only when every failed check is advisory; use NOT_READY for mandatory failures and READY for supported continuation, retaining truthful typed check evidence. The gate's treatment of PARTIAL is unchanged until removal. There are 20 preflight rules, all static, with 4 BLOCK and 16 WARN (F017 and F018 are retired and never fire); the generated [catalog page](rules/preflight.md) is the source of truth for tiers.

**Conformance and tests are separate measures.** Conformance checks that the required preflight scenarios are *defined*; whether they *pass* is the test gate's measure. No preflight rule executes tests, syncs an environment or reads test results, and a conformance run where every scenario is defined is a pass whatever state the tests are in.

F006–F015 and F019 add static diagnostics for handler types, actionable errors, raised expected failures, verdict consistency, entrypoint inputs, blocking probes, budgets, cleanup, sensitive failure text, removed gate configuration, and incomplete analysis. F020 flags a suppression that still cites a retired id. Existing F001–F004 and F005 continue to apply. F001, F003, F006 and F007 use BLOCK (SARIF `error`). F003/F006/F007 enforce typed failures, handler contracts, and definite missing failure guidance. F016 warns while the fleet registers its scenarios and is promoted to BLOCK once it has. A fully defined F016 matrix also clears the F019 findings whose unresolved property every F016 scenario asserts — a computed aggregation, an expanded failure constructor, an unresolved error expression, a dynamic `passed`; an F019 that reports code the analysis never reached (unparsed file, unresolved `preflight_check`, dynamic callback binding, unresolved input contract) is not clearable that way. See the preflight guide's F019 section. Other preflight rules remain WARN because their findings include heuristics, unresolved analysis, or SDK-version-dependent advice. `--exit-zero` preserves error findings while returning a successful process exit for soft enforcement.

Run the checks:

```bash
uv run atlan-application-sdk-conformance detect --repo . --series F --output preflight.sarif
```

`--with-tests`, `--preflight-report`, `--test-timeout` and `--test-python` are deprecated no-ops, removed in v0.40.0; so are the `with-tests` input of the reusable conformance workflow and the pytest plugin's `--preflight-report` / `--preflight-rules` options.

## Defining a scenario

F016 applies to an app that defines its own `preflight_check`. It reads the tests statically and never runs them. A scenario is defined when a pytest-collected test — a `test*` function, or a `test*` method of a `Test*` class, in a `test_*.py` or `*_test.py` module under `tests/unit/` — carries `pytest.mark.preflight_conformance(rule="F016", scenario="healthy", entrypoint="crawler")`, runs, and calls the contract assertion the scenario needs in a position F016 accepts. Only `tests/unit/` counts: it is the tier the test gate's unit job always runs, and a scenario defined anywhere else is not guaranteed to execute. `entrypoint` defaults to `"default"`. The authoritative scenario names are in `conformance.preflight_testing.SCENARIOS`. Each `@entrypoint` the app declares needs the complete matrix; an app with none owes it once, for `default`.

F016 reads an allowlist of shapes — the ones atlan-openapi-app, atlan-mysql-app and atlan-metabase-app use — and reports anything else rather than modelling it. A shape outside the list is never credited, however it would behave at runtime; rewriting the test into a listed shape is the fix.

The marker can decorate the test directly, or ride on a `pytest.param` inside `pytest.mark.parametrize` to register one scenario per entrypoint. The parametrize may come from a module-level helper whose body is a single `return`, and its parameter list may be a comprehension over a module-level tuple of names — atlan-metabase-app's `entrypoint_matrix(scenario)` is the reference shape. When the parametrize supplies an `entrypoint` argument, each case's value must be the entrypoint its marker claims. The marker's `rule`, `scenario` and `entrypoint` must resolve to literal strings; a registration the static reader cannot resolve is reported, not counted.

A registration does not define its scenario when the test does not run: `skip`, a `skipif` or `xfail` whose condition is literally true (or absent), a function-level marker on a parametrize with no runnable case, or an unconditional `pytest.skip()` / `pytest.xfail()` at the top of the body — from a decorator, a class decorator, a module or class `pytestmark` (plain, annotated, chained or augmented), or a param-level mark. A literally false condition runs. A condition the reader cannot evaluate — an expression, a name, or a string pytest evaluates itself — and an unreadable `pytestmark` element leave the run state unknown, so the scenario is reported and not counted. It also does not count when it declares `unsupported=True`, names a scenario or entrypoint outside the matrix, or does not call the required assertion in an accepted position.

Other decorators: any other `pytest.mark.*` does not affect whether the test runs, and a decorator imported from another module (`respx.mock`, `mock.patch("...")`) is taken not to skip it. A decorator defined in the test module itself — a function other than a registration helper, a lambda, a module-level mark alias such as `skip_ci = pytest.mark.skipif(...)` — is not read, so the test's run state is unknown and its scenarios are reported and not counted. Put the mark on the test directly instead.

The assertion must be `conformance.preflight_testing`'s — imported by name (aliases included) or reached through the module (`import conformance.preflight_testing as pt`, `from conformance import preflight_testing`) — and called as a statement of the test body, or of a top-level `for` loop over a provably non-empty iterable: a non-empty list or tuple literal, a name bound once to one (in the test, or at module level), `range(n)` with `n >= 1`, or `enumerate` of any of those. No statement before it may `return`, `yield`, leave the loop with `break`/`continue`, or call `pytest.skip`/`xfail`/`exit`/`importorskip`, because those are the ways a test can pass without making the call. A statement that fails the test first — an exception, a `pytest.raises` block that saw none — does not withhold credit: the test fails, and the test gate reports it. A call made by a helper, inside `if`/`try`/`with`/a nested function, or as part of a larger expression is not counted.

A name resolves only when the module binds it exactly once and the test does not rebind it (a fixture parameter or local of the same name shadows it). A second import, definition or assignment of the name anywhere at module level — even in a dead branch or an `except ImportError:` fallback — leaves it unresolved, and a star import leaves every name unresolved.

Where a parametrize supplies `entrypoint` — to the case carrying the marker, or through another stacked parametrize — a registration counts only if a runnable case supplies the entrypoint its marker claims. Any other known value (including `None`) is a mismatch, and an unreadable one leaves it unresolved.

Load the plugin with `pytest -p conformance.preflight_testing` (or declare the `preflight_conformance` marker in your pytest config) so the marker is registered.

## Writing the scenario

F016 tests invoke the real handler using an app-owned source adapter and call `assert_preflight_result` with the required checks, observed probes, and expected status. For failed checks, the assertion requires typed errors with nonblank codes, messages, and suggested actions. Provide `expected_errors` to verify classification and `synthetic_secrets` with captured logs to check redaction. Model typing establishes category, audience, and retryability types. Human review still establishes whether an action is useful and appropriate to its audience.

`hung_probe`, `cancellation_cleanup`, and `budget_retry` additionally require `assert_probe_lifetime` using measured elapsed time and independent evidence that background work stopped. Test recovered transients against extraction's actual retry or fallback under the same injected failure. Do not infer safe continuation solely from a retryable error.

`assert_extraction_scheduled` and `assert_preflight_exit` served the retired F017 and F018 rules. They are deprecated and removed in v0.40.0; the gate behaviour they checked is asserted in the SDK's own tests.

These assertions validate supplied observations. They cannot establish that a test actually called the production handler or measured a real timeout. Review the adapters alongside the tests. Scenario registration is a definition check, not an automatic implementation of source-specific tests.

Static analysis follows directly resolvable helpers and imported error inheritance. Dynamic dispatch, arbitrary client instances, custom construction, decorator wrappers, and recovery semantics can exceed its analysis. A clean static report is not runtime certification. PR #3685's target gate behavior must be verified on its actual implementation; this package does not change SDK gate behavior or invent a release floor for its migration warnings.
