<!-- AUTO-GENERATED — do not edit this file directly.
     Source of truth: conformance/suite/rules/dependency.py
     To regenerate:  uv run atlan-application-sdk-conformance gen-rule-docs
     To check CI staleness: uv run atlan-application-sdk-conformance gen-rule-docs --check -->

# D012 · Dependency Rules (D-series)

Single-rule view — the full series catalog lives at [`dependency.md`](../dependency.md#d012).

Suppress a finding on the violating line or the line directly above it:

```python
# conformance: ignore[D012] intentional: <why this is deliberate here>
```

## D012 — `UnpinnedPackageIndex` {#d012}

**Tier:** `warn` · **Scope:** `both` · **Fix belongs in:** `packaging` · **Category:** `supply-chain` · **Autofixable:** yes · **Since:** 0.30.0

> pyproject.toml does not pin PyPI as the default uv index, so a machine-wide index can rewrite uv.lock

**Rationale:** Atlan laptops carry a machine-wide uv default index — the Endor Labs package firewall,
installed into ~/.config/uv/uv.toml with 'default = true' and credentials inline. A repo
that does not pin its own default index inherits it, and then every 'uv run', 'uv sync'
and 'uv lock' rewrites every package URL in uv.lock to the proxy host. Note that 'uv
run' alone is enough — the rewrite does not wait for a deliberate lock operation, and
'uv lock --upgrade-package X' does not scope it either, because the index override is
machine-level rather than lock-level. The rewrite is silent: it prints nothing and it
touches a file the task at hand never mentioned, so it is noticed only by someone who
happens to read the diff. A project-level index takes precedence over the machine-wide
one and every uv subcommand reads it, so one stanza closes the whole class. It can also
move a VERSION, which is the part most likely to be missed: the proxy mirror lags PyPI,
so resolving through it can silently DOWNGRADE a package that was already correctly
pinned. Observed on atlan-metabase-app (FND-2547): a single 'uv run' took
atlan-application-sdk-conformance from 0.36.1 back to 0.36.0 while rewriting all 150
sources. A reader who trusts that only URLs move will not think to check the versions.
Customer impact: the rewritten lock is committed and CI, which holds no credential for
the proxy, fails at dependency install with 401 Unauthorized on a different package each
run. The connector's release stalls behind a failure that reads as an outage in someone
else's infrastructure, and the cause is invisible in the diff that produced it.

### What correct looks like

- **Compliant example:** atlan-mysql-app pyproject.toml — `[[tool.uv.index]]` names pypi at
  https://pypi.org/simple with `default = true`. Declared in pyproject.toml rather than
  a project-level uv.toml, so the repo's [tool.uv] constraint-dependencies keep being
  read, and a machine-wide index cannot rewrite uv.lock on whoever resolves next.

The repo's root `pyproject.toml` must declare PyPI as the resolver's default index:

```python
[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
default = true
```

Two branches, reported at most once per repo:

1. **No default index** — no `[[tool.uv.index]]` entry is    marked `default = true`, so
whatever default the machine    supplies is what resolves. 2. **Default index is not
PyPI** — an entry is marked default    but its URL names some other host, which is the
proxy rewrite    already committed into the pyproject.

Anchored on the root `pyproject.toml` only: this is a property of the repo's resolution,
not of each sub-package, so a monorepo gets one finding rather than one per member.

The pin belongs in `pyproject.toml`, **not** in a project-level `uv.toml`.  A `uv.toml`
does override the user-level file, but it also suppresses `[tool.uv]` in
`pyproject.toml` entirely — silently dropping any `constraint-dependencies` CVE floors
the repo declares there.  uv warns about that, but names only `constraint-dependencies`.

Scope is `both`: the SDK inherits the machine-wide index exactly as an app does.  This
rule pins the index; `D013` checks whether a non-PyPI host has already reached
`uv.lock`.  Cite: FND-1928.
