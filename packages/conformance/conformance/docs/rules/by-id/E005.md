<!-- AUTO-GENERATED — do not edit this file directly.
     Source of truth: conformance/suite/rules/error_handling.py
     To regenerate:  uv run atlan-application-sdk-conformance gen-rule-docs
     To check CI staleness: uv run atlan-application-sdk-conformance gen-rule-docs --check -->

# E005 · Error-Handling Rules (E-series)

Single-rule view — the full series catalog lives at [`error-handling.md`](../error-handling.md#e005).

Suppress a finding on the violating line or the line directly above it:

```python
# conformance: ignore[E005] intentional: <why this is deliberate here>
```

## E005 — `ExceptBlockMissingExcInfo` {#e005}

**Tier:** `warn` · **Scope:** `both` · **Fix belongs in:** `app` · **Category:** `missing-traceback` · **Autofixable:** yes · **Since:** 0.2.0

> except block logs without exc_info=True — stack trace discarded

**Rationale:** Tracebacks are the primary artifact of incident postmortems. A record that says
something failed but carries no stack trace forces engineers to reproduce the failure —
often impossible under production data volumes.

The message is logged but the stack trace is lost.  Add `exc_info=True` to every
`logger.warning()` / `logger.error()` call inside an except block.  `logger.exception()`
is exempt (it implies `exc_info=True`).

Exempt: calls whose arguments flow through a recognised redaction helper
(redact*/sanitiz*/safe_traceback/scrub_secret*/mask_secret*) — these mark a deliberate
no-traceback boundary where exc_info=True would serialize the raw exception past the
sanitizer and can leak credentials (JDBC URLs, Authorization headers, OAuth bodies).
