atlan:
  deployment_name: "ci-${CI_RUN_SUFFIX}-sdr"
  domain: "${SDR_TEST_TENANT}"
  client_id: "${SDR_CLIENT_ID}"
  client_secret: "${SDR_CLIENT_SECRET}"

objectstore:
  type: bindings.localstorage
  version: v1
  metadata:
    - name: rootPath
      value: "/app/data"

secretstore:
  type: secretstores.local.env
  version: v1
