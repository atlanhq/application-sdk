version: 2
updates:
  # pip ecosystem disabled - requirements.txt exists only for Snyk scanning
  # Dependency management is handled via uv/pyproject.toml

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: weekly
