version: 2
updates:
  - package-ecosystem: "pip"   # or another ecosystem like "maven", "nuget", etc.
    directory: "/"             # location of your manifest file
    schedule:
      interval: "daily"
    target-branch: "develop"      # updates are opened against the main branch

