{
  "execution_mode": "automation-engine",
  "dag": {
    "extract": {
      "activity_name": "execute_workflow",
      "activity_display_name": "Extract Deploy Config Example Metadata",
      "app_name": "deploy",
      "inputs": {
        "workflow_type": "deploy",
        "app_name": "deploy",
        "app_id": "",
        "argo_workflow_slug": "",
        "task_queue": "atlan-deploy-{deployment_name}",
        "args": {
          "credential": "{{credential}}",
          "agent_json": "{{agent-json}}",
          "target": "{{target}}"
        }
      },
      "error_handling": {
        "start_to_close_timeout_seconds": 86400
      }
    }
  }
}
