{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "command": "root=$(git rev-parse --show-toplevel); staged=$(git -C \"$root\" diff --name-only --cached); for i in 1 2 3; do (cd \"$root\" && uv run pre-commit run) && exit 0; printf '%s\\n' \"$staged\" | while IFS= read -r f; do [ -n \"$f\" ] && git -C \"$root\" add -- \"$f\"; done; done; exit 2",
            "statusMessage": "Running pre-commit checks..."
          }
        ]
      }
    ]
  }
}
